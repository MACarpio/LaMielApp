@model LaMielApp.Models.Pago
@{
  ViewData["Title"] = "boleta";
}

<div class="seccion-titulo">
  <h5 class="titulo-seccion">@ViewData["Message"]</h5>
</div>
<div class="seccion-titulo text-center">
  <h5 class="titulo-prim">Generar boleta</h5>
  <h5 class="precio-producto">@Html.DisplayFor(model => model.Nombres)</h5>
  <a class="btn-prim" asp-controller="Proforma" asp-action="Documento" asp-route-id="@Model.Id"> Boleta </a>
</div>